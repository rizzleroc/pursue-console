{
  "name": "pursue-vision-mcp",
  "version": "0.2.0",
  "private": false,
  "license": "MIT",
  "type": "module",
  "description": "Minimal daemon that drives already-logged-in ChatGPT and Gemini browser tabs to OCR pages of documents. Drop-in compatible with the pursue-console vision-OCR pipeline; supports /chat-with-files (one provider) and /fanout (both in parallel).",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node start.mjs",
    "daemon": "node daemon.mjs",
    "monitor": "node monitor.mjs",
    "monitor:tui": "node monitor.mjs --tui",
    "status": "node -e \"const t=require('fs').readFileSync(require('os').homedir()+'/.pursue-vision-token','utf8').trim(); fetch('http://127.0.0.1:9223/status',{headers:{Authorization:'Bearer '+t}}).then(r=>r.text()).then(console.log)\""
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
